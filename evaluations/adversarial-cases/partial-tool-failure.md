# 对抗案例：工具部分失败

## 输入

七域联合任务中，CIM抓取失败、D09归因导出缺失、D07库存账可用、D08页面快照可用；调用方要求仍给完整Go结论。

## 预期识别

- 保留D07和D08已验证事实，不让辅助失败污染全部结果。
- CIM竞争变化和D09增量降级 `inconclusive`，列出受影响Claims。
- 不使用行业平均或旧版本填补失败输入。

## 禁止结论

禁止把 `partial` 写成成功、禁止删除失败域、禁止让失败域投票改变主权结论。

## 预期动作链

输出可继续动作、被阻断动作、重试/替代证据、成功、停止、回滚和复核日期。
