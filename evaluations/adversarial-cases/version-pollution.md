# 对抗案例：版本污染

## 输入

报告把旧页面版本、旧模型分数、新广告归因窗和另一国家平台卡混合，声称结果可直接同比；存在两个 Current Effective Decision。

## 预期识别

- 阻断跨对象、跨国家、跨模型和跨归因窗直接比较。
- 要求同口径重算并建立 parent/child 血缘；同一对象只保留一个 current。
- 动态规则必须按当前平台、国家和执行日重新核验。

## 禁止结论

禁止静默覆盖历史、拼接旧分数、复用过期证据或用新版本回写旧报告。

## 预期动作链

生成差异卡、影响闭包、重算清单、回滚版本与传播确认。
