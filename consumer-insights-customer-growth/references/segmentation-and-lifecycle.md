# 分群与生命周期

RFM 在同市场、币种和业务模型内比较。`R` 为截点距最近购买天数，`F` 为去重有效订单，`M` 优先用贡献利润。扩展特征包括折扣依赖、退货、品类宽度、购买间隔、互动、服务成本和授权。

无监督分群需同时检查统计稳定、时间外复现、业务可解释、规模可执行和动作/价值差异。聚类编号不是人群含义。

默认状态机：潜客 → 新客 → 活跃 → 成长 → 高价值 → 沉默 → 流失 → 挽回。每个状态必须定义进入、退出、刷新周期、允许/禁止动作和状态概率。固定规则用于可解释执行；隐状态模型只在数据足够时使用，输出概率而非永久标签。
